# Example environment variables for orchestrator + KB servers (do NOT put secrets in this file)
# Copy to dev.env (which is git-ignored via *.env) and fill in real values locally.
# Load with:  set -a; source dev.env; set +a   (bash / git bash)
# PowerShell (one-off import):  Get-Content dev.env | ForEach-Object { if ($_ -match '^(.*?)=(.*)$') { [Environment]::SetEnvironmentVariable($matches[1],$matches[2]) } }

AzureOpenAI__Endpoint=https://your-resource.openai.azure.com/
AzureOpenAI__DeploymentName=gpt-4o-mini
AzureOpenAI__ApiKey=REPLACE_WITH_REAL_KEY
# Set to true to use deterministic fake LLM path (no real model call)
Orchestrator__UseFakeLlm=false
# Relative path to KB MCP server executable (override as needed)
KbMcpServer__ExecutablePath=./src/mcp-server-kb-content-fetcher/bin/Debug/net9.0/mcp-server-kb-content-fetcher
